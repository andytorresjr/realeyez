{
  "name": "realeyez",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "node --check packages/core/src/feedRanker.js && node --check packages/core/src/provenance.js && node --check packages/core/tests/feedRanker.test.js && node --check packages/core/tests/provenance.test.js",
    "test": "node --test packages/core/tests/*.test.js",
    "mobile": "npm run start --workspace @realeyez/mobile"
  }
}
